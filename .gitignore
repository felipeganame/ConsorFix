# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.turbo/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
logs/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json.example
.idea/

# Test/coverage
coverage/
.nyc_output/

# Expo / RN
.expo/
.expo-shared/
*.apk
*.ipa
android/build/
ios/build/

# Storage local de archivos subidos en dev.
# Anclados a la raíz con '/': sin el slash inicial, git aplica el patrón a
# CUALQUIER directorio con ese nombre a cualquier profundidad, y se comía
# `apps/api/src/storage/` — el módulo del servicio de S3 nunca entró al repo y
# el CI falló con "Cannot find module". Es el mismo error que tenía el patrón de
# .claude-flow/data/.
/storage/
/uploads/

# Ruflo runtime
# Con `**/` para que aplique también en apps/* y packages/*: un patrón con
# slash interno queda anclado a la raíz y no matchea en subdirectorios.
**/.claude-flow/data/
**/.claude-flow/logs/
**/.claude-flow/sessions/
.claude/memory.db
.claude/memory.db-*
.swarm/
ruvector.db
ruvector.db-*

# Notas de trabajo locales (borradores de PR, apuntes de sesión).
# Se quedan en disco pero no se versionan.
falta.txt
pr-fase5.txt
notas-*.txt

# Misc
*.tgz
